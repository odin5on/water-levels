# Get a free key at https://api.waterdata.usgs.gov/signup/
# The key is visible in browser requests — that's acceptable for USGS (rate-limited, not secret).
# Rotate by generating a new key and updating this var.
VITE_USGS_API_KEY=
